// Given an array of integers, write a program to find the maximum difference between any two
// elements.

let arr =[23,56,34,1,8]
console.log(newArr = arr.sort((a,b)=>{
    return a-b
}))

console.log(maxDiff = newArr[newArr.length -1] - newArr[0])