// OPERATORS

// Unary       increment,decrement(postfix,prefix)
// Arithmetic  +,-,/,%,**
// Shift       left shift(>>),right shift(<<)
// Relational  <,><=,>=,==(allow type coercion),===(does'n allow type coercion)
// Bitwise     
            // & - bitwise and,returns 1 if both bits are 1 else 0
            // | - bitwise or,returns 1 if either bits is 1 else 0
            // ^ - bitwise xor,returns 1 if both bits are different else 0
            // ~ - bitwise not,changes 1 to 0 and vice versa.
// Logical     &&, ||, ! 
// Assignment  =
// Ternary     ?=
            // alternative of if else


